import * as loginService from '../../services/login';
import { routerRedux } from 'dva/router';

export default {
    namespace: 'node',
    state: {
    },
    subscriptions: {
    },
    effects: {
    },
    reducers: {
    },
}